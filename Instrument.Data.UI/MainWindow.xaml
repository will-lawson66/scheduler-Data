<Window x:Class="Instrument.Data.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Instrument.Data.UI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        mc:Ignorable="d"
        Title="Instrument Scheduler Manager" Height="750" Width="1200" 
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryDark" Padding="16" materialDesign:ShadowAssist.ShadowDepth="Depth2">
            <TextBlock Text="Instrument Scheduler Manager" 
                       Foreground="White" 
                       FontSize="24" 
                       FontWeight="Bold"/>
        </materialDesign:ColorZone>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Navigation Menu -->
            <materialDesign:Card Grid.Column="0" materialDesign:ShadowAssist.ShadowDepth="Depth1" Margin="0" Background="White">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,20,0,0">
                        <TextBlock Text="ENTITIES" 
                                   Margin="16,0,0,8" 
                                   Foreground="{DynamicResource MaterialDesignBodyLight}" 
                                   FontSize="12"/>
                        
                        <ListBox x:Name="NavigationItems" SelectedIndex="{Binding SelectedViewIndex, Mode=TwoWay}" Margin="0,8">
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Sequence" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Sequences" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Function" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Parameters" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ListStatus" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Ranges" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ToolboxOutline" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Resources" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FolderMultipleOutline" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Sequence Groups" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>
                        
                        <Separator Margin="16,8" />
                        
                        <TextBlock Text="TOOLS" 
                                   Margin="16,8,0,8" 
                                   Foreground="{DynamicResource MaterialDesignBodyLight}" 
                                   FontSize="12"/>
                        
                        <ListBox Margin="0,8">
                            <ListBoxItem Command="{Binding NavigateToVisualizerCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="GraphOutline" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Relationship Visualizer" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Command="{Binding ImportCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Import" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Import Data" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Command="{Binding ExportCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Export" Width="24" Height="24" Margin="4,0,8,0" />
                                    <TextBlock Text="Export Data" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>
                    </StackPanel>
                </ScrollViewer>
            </materialDesign:Card>
            
            <!-- Content Area -->
            <Grid Grid.Column="1">
                <materialDesign:Card Margin="16" Padding="16" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                    <ContentControl x:Name="MainContent"/>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>
    
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Rectangle x:Name="indicator" 
                                         Width="4" 
                                         HorizontalAlignment="Left" 
                                         Fill="{StaticResource PrimaryBrush}" 
                                         Visibility="Collapsed"/>
                                <ContentPresenter Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f5f5f5"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="border" Property="Background" Value="#f0f0f0"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
