<Window x:Class="Instrument.Data.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Instrument.Data.UI"
        mc:Ignorable="d"
        Title="Instrument Scheduler Manager" Height="750" Width="1200" 
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="16">
            <TextBlock Text="Instrument Scheduler Manager" 
                       Foreground="White" 
                       FontSize="24" 
                       FontWeight="Bold"/>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Navigation Menu -->
            <Border Grid.Column="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="ENTITIES" 
                               Margin="16,0,0,8" 
                               Foreground="{StaticResource TextLightBrush}" 
                               FontSize="12"/>
                    
                    <Button Command="{Binding NavigateToSequencesCommand}" 
                            Style="{StaticResource NavButtonStyle}"
                            Content="Sequences"/>
                    
                    <Button Command="{Binding NavigateToParametersCommand}" 
                            Style="{StaticResource NavButtonStyle}"
                            Content="Parameters"/>
                    
                    <Button Command="{Binding NavigateToRangesCommand}" 
                            Style="{StaticResource NavButtonStyle}"
                            Content="Ranges"/>
                    
                    <Button Command="{Binding NavigateToResourcesCommand}" 
                            Style="{StaticResource NavButtonStyle}"
                            Content="Resources"/>
                    
                    <Button Command="{Binding NavigateToSequenceGroupsCommand}" 
                            Style="{StaticResource NavButtonStyle}"
                            Content="Sequence Groups"/>
                    
                    <TextBlock Text="TOOLS" 
                               Margin="16,20,0,8" 
                               Foreground="{StaticResource TextLightBrush}" 
                               FontSize="12"/>
                    
                    <Button Command="{Binding NavigateToVisualizerCommand}" 
                            Style="{StaticResource NavButtonStyle}"
                            Content="Relationship Visualizer"/>
                </StackPanel>
            </Border>
            
            <!-- Content Area -->
            <Border Grid.Column="1" Padding="20">
                <ContentControl x:Name="MainContent"/>
            </Border>
        </Grid>
    </Grid>
    
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Rectangle x:Name="indicator" 
                                         Width="4" 
                                         HorizontalAlignment="Left" 
                                         Fill="{StaticResource PrimaryBrush}" 
                                         Visibility="Collapsed"/>
                                <ContentPresenter Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f5f5f5"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="border" Property="Background" Value="#f0f0f0"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
